name: Publish Site

on:
  push:
    branches:
      - master

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
        with:
          submodules: true
      - name: Publish Site
        uses: chabad360/hugo-gh-pages@master
        with:
          githubToken: ${{ secrets.HUGO_TOKEN }}
          branch: main
          repo: https://github.com/seoah-lab/seoah-lab.github.io
          args: --gc --minify --cleanDestinationDir
